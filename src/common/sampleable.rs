// use crate::{math::Real, samplers::Sampler};

// pub enum SamplingMethod<P, S> {
//   Continuous(Box<dyn Fn(&P, &mut dyn Sampler) -> S>, Box<dyn Fn(&P, &S) ->
// Option<PositiveReal>>),   DiracDelta(Box<dyn Fn(&P, &mut dyn Sampler) -> S>)
// }
